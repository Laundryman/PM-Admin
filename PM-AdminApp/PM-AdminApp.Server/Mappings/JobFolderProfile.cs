using AutoMapper;
using PlanMatr_API.Mappings.Resolvers;
using PMApplication.Dtos;
using PMApplication.Dtos.PlanModels;
using PMApplication.Entities.JobsAggregate;
using PMApplication.Entities.PlanogramAggregate;

namespace PM_AdminApp.Server.Mappings
{
    public class JobFolderProfile : Profile
    {
        //public JobFolderProfile() {
        //    CreateMap<JobFolder, JobFolderDto>()
        //        .ForMember(j => j.Id, opt => opt.MapFrom(x => x.Id))
        //        .ForMember(j => j.Name, opt => opt.MapFrom(x =>x.Name))
        //        .ForMember(j => j.Description, opt => opt.MapFrom(x => x.Description))
        //        .ForMember(j => j.ParentFolderId, opt => opt.MapFrom(x => x.ParentFolderId))
        //        .ForMember(j => j.BrandId, opt => opt.MapFrom(x => x.BrandId))
        //        .ForMember(j => j.RegionId, opt => opt.MapFrom(x => x.RegionId))
        //        .ForMember(j => j.Jobs, opt => opt.MapFrom(x => x.Jobs))
        //        .ForMember(j => j.ParentFolderId, opt => opt.MapFrom(x => x.Description))
        //        //.ForMember(j => j.Region, opt => opt.Ignore())
        //        //.ForMember(j => j.Countries, opt => opt.Ignore())






        //}

    }
}
